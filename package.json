{
  "name": "ci-shard-advisor",
  "version": "0.1.0",
  "private": true,
  "description": "Analyze Playwright JSON reports and recommend the optimal CI sharding strategy based on execution time, workers and cost.",
  "scripts": {
    "test": "pnpm --recursive test",
    "typecheck": "pnpm --recursive typecheck"
  }
}
